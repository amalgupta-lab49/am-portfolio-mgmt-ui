import DarkSchema from './darkSchema.json';
import RedSchema from './redSchema.json';
import BlueSchema from './blueSchema.json';
import GreenSchema from './greenSchema.json';

export default {
    "plain": {
        "darkPlain": DarkSchema.plain,
        "redPlain": RedSchema.plain,
        "bluePlain": BlueSchema.plain,
        "greenPlain": GreenSchema.plain,
    },
    "gradient": {
        "darkGradient": DarkSchema.gradient,
        "redGradient": RedSchema.gradient,
        "blueGradient": BlueSchema.gradient,
        "greenGradient": GreenSchema.gradient,
    }
};